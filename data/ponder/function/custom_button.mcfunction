

## you can call this function with custom triggers 
# e.g. you have custom block with gui and you want to add in ui button for help
#

# to add new button add new function with the command : 

    # data merge storage ponder:id {ponder_id:"NAMESPACE:DIALOG"}
     # and then then call this function or run the command below

         # make sure to replace "NAMESPACE:DIALOGNAME" with valid dialog 

function ponder:open_ponder with storage ponder:id
